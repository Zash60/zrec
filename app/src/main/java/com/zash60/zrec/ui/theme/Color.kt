package com.zash60.zrec.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color palette from DESIGN.md — warm near-black terminal aesthetic.
 */
object ZrecColors {
    // Primary
    val OpenCodeDark = Color(0xFF201d1d)
    val OpenCodeLight = Color(0xFFfdfcfc)
    val MidGray = Color(0xFF9a9898)

    // Secondary
    val DarkSurface = Color(0xFF302c2c)
    val BorderGray = Color(0xFF646262)
    val LightSurface = Color(0xFFf1eeee)

    // Accent
    val AccentBlue = Color(0xFF007aff)
    val AccentBlueHover = Color(0xFF0056b3)
    val AccentBlueActive = Color(0xFF004085)

    // Semantic
    val DangerRed = Color(0xFFff3b30)
    val DangerHover = Color(0xFFd70015)
    val SuccessGreen = Color(0xFF30d158)
    val WarningOrange = Color(0xFFff9f0a)
    val WarningHover = Color(0xFFcc7f08)

    // Text
    val TextMuted = Color(0xFF6e6e73)
    val TextSecondary = Color(0xFF424245)

    // Border
    val BorderWarm = Color(0x1F0F0000) // rgba(15, 0, 0, 0.12)
}
